---
description: Takt 项目架构与整体规范（含项目总则）
alwaysApply: true
---

# Takt 项目架构

节拍数字工厂 · Takt Digital Factory (TDF)，前后端分离的一体化企业级后台脚手架，支持 RBAC 权限、多租户、多数据库分库。

## 项目结构

```text
Takt.Net/
├── backend/                        # .NET 8 后端
│   ├── src/
│   │   ├── Takt.Domain/           # 领域层：实体、仓储接口、领域接口、验证
│   │   ├── Takt.Application/      # 应用层：DTO、应用服务
│   │   ├── Takt.Infrastructure/   # 基础设施：SqlSugar、多库、仓储实现、种子、Swagger 等
│   │   ├── Takt.Shared/           # 共享：通用模型、异常、帮助类
│   │   └── Takt.WebApi/           # Web API：控制器、Program、配置说明
│   └── Takt.Net.sln               # 后端解决方案
├── frontend/
│   └── takt.antd/                 # Vue3 + Ant Design Vue 前端
│       ├── src/
│       │   ├── views/             # 页面组件
│       │   ├── api/               # API 封装（与后端控制器一一对应）
│       │   ├── types/             # DTO 对应的 TypeScript 类型
│       │   ├── stores/            # Pinia 状态
│       │   ├── locales/           # i18n 文案
│       │   └── router/            # 路由
│       └── package.json
└── .cursor/rules/                 # 本项目规则文件（见下文）
```

## 文档与配置

- 后端配置说明：`backend/src/Takt.WebApi/配置说明.md`（包含租户、多库、Swagger、日志等配置要点）。
- 后端 Swagger 文档：
  - 通过 `Takt.Infrastructure.Extensions.TaktSwaggerCollectionExtensions.AddTaktSwagger` 注册；
  - 在 `appsettings.json` 中启用 `"Swagger": { "Enabled": true }` 后，通过 `/swagger` 访问 UI。

## 技术栈

| 层级 | 技术 |
|------|------|
| 后端 | .NET 8、C#、SqlSugarCore、Swashbuckle、Serilog、多租户/多库（ConfigId 路由） |
| 前端 | Vue 3、Vite、Ant Design Vue、TypeScript、Pinia、Axios |

## 项目总则

- **结论基于真实代码**：禁止“推测”“可能”“预测”等模糊表述；修改前需亲自核对代码。
- **信息源标注**：区分来自聊天上下文与来自工程/代码的信息。
- **保持原有功能（强制）**：任何新增或修改都不得删除、覆盖或破坏已有功能。
- **回答语言**：使用中文简体。
- **不需要额外说明文档**：除非用户明确要求。
- **编码与工具策略（强制）**：涉及中文读写、检索、编辑时，统一优先使用内置编辑与检索工具；当内置工具无法满足且必须使用 Python/PowerShell 时，仅可在 Windows 环境先完成 UTF-8 编码设置与中文无乱码校验后执行。未完成编码校验前，禁止任何中文相关批量处理或文件改写。凡涉及批量修改，必须先获得用户明确同意后方可执行。

## 命名与结构补充

- **后端**：公开类型（类、接口、枚举）统一使用 **Takt** 前缀；命名空间与文件夹按领域划分（如 Identity、HumanResource.Organization、Statistics.Logging、Routine.Dict）。
- **前端**：API 路径与后端控制器一致（如 `/api/TaktDepts`）；类型与接口与后端 DTO 对应，使用 camelCase；目录与后端模块对应（api/、types/、views/、locales/ 按模块分）。
- **文件头（可选但推荐）**：C# 与 TS/Vue 文件可保留项目统一文件头格式（项目名称、命名空间/路径、文件名称、功能描述、版权与免责声明）。

## 多租户与数据库

- 实体通过 `TaktEntityDatabaseMapping.GetConfigIdByEntityNamespace` 按命名空间分配 ConfigId；新增实体需在 `TaktEntityDatabaseMapping` 中符合现有映射规则（ConfigId 0~5 对应 Identity/Accounting/HumanResource/Logistics/Generator-Routine/Routine）。
- 实体基类为 `TaktEntityBase`，包含 Id(long)、ConfigId、软删除及审计字段；Id 序列化使用 `ValueToStringConverter` 避免前端精度问题。

## 开发约定

- **数据设计规则**：实体/数据库命名、多库 ConfigId 映射等，见 `01-data-design.mdc`。
- **后端规则**：
  - 架构与分层、控制器/服务/DTO、租户/种子、后端文档与注释约定：**统一见 `02-backend.mdc`**。
  - **本地化**：每个继承 `TaktEntityBase` 的业务实体对应 **一个** `Takt{Xxx}EntitiesSeedData` 并在 `TaktSeedsCollectionExtensions` 双注册；细则见 `02-backend.mdc`「实体翻译种子」。
- **前端规则**：
  - Vue/TS 工程结构、API 命名、代码风格、类型与视图约定：**统一见 `03-frontend.mdc`**。
> 新增规则文件时，保持与本文件相同的结构与分层风格，便于在多个项目间复用。
