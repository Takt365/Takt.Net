export default {
  /** 仅包含前端必填校验提示，由 IsRequired 驱动 */
{{~ for col in columns ~}}
{{~ if col.is_required == 1 ~}}
  required_{{ col.ts_column_name }}: '{{ if col.html_type == "select" && col.dict_type }}请选择{{ col.comment }}{{ else }}请输入{{ col.comment }}{{ end }}',
{{~ end ~}}
{{~ end ~}}
}
