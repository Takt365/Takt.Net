/**
 * Leave entity · English
 * - Entity name, search keyword, field labels from backend entity translation (entity.leave.xxx)
 * - Here: only module-specific tabs, validation messages if any
 */
export default {}
