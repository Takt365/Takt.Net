/**
 * Employee entity · English
 * - Page title, entity name, search keyword from backend entity translation (entity.employee.xxx)
 * - Here: only module-specific tabs, validation messages if any
 */
export default {
  employeeCodeCreateHint: 'Generated on save (male: prefix 1…, female: prefix 2…)',
  employeeCodeSystemHint: 'Generated on save using the user code (system) rule',
  systemEmployeeCode: 'User code (system)',
  systemEmployeeCodeHint: 'Only administrators can enable; for accounts that will bind admin users',
  genderRequiredForD12: 'Select male or female to generate the employee code'
}
